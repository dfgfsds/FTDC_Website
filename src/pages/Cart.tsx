import CartPage from '../components/Cart/CartPage'

interface Props { }

function Cart(props: Props) {
    const { } = props
  
       
    return (
        <CartPage />
    )
}

export default Cart
